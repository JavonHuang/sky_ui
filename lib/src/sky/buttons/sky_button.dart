part of 'buttons.dart';

class SkyButton extends StatelessWidget {
  const SkyButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: ElevatedButton(
        onPressed: () {},
        child: Text('89989'),
      ),
    );
  }
}
