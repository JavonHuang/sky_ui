part of "index.dart";

class SkyCol extends StatelessWidget {
  const SkyCol({
    super.key,
    this.span,
    this.width,
    this.height,
  });
  final SkyColSpan? span;
  final double? width;
  final double? height;

  @override
  Widget build(BuildContext context) {
    return Text("8989");
  }
}
